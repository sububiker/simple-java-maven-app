pipeline {
    agent {
           sh 'echo start pipeline now'
    }
    stages {
        stage('Build') { 
            steps {
                sh 'mvn -B -DskipTests clean package' 
            }
        }
    }
}
